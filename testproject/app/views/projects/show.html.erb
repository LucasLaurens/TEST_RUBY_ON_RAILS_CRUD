<div class="mt-2 mb-2">
	<%= link_to 'Back', :back, class: 'btn btn-warning' %>
</div>
<table class="table table-striped">
	<thead>
		<tr>
			<td>Name</td>
			<td>Description</td>
		</tr>
	</thead>
	<tbody>
			<tr>
				<td><%= @project.name %></td>
				<td><%= @project.description %></td>
			</tr>
	</tbody>
</table>